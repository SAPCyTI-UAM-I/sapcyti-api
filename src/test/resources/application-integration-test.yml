spring:
  datasource:
    url: jdbc:h2:mem:sapcyti-auth;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      data-locations: classpath:integration-test/data.sql
  flyway:
    enabled: false
  mail:
    host: localhost
    port: 1025

app:
  password-reset:
    base-url: http://localhost:4200
    ttl-minutes: 30

jwt:
  private-key-path: classpath:jwt/dev-private.pem
  public-key-path: classpath:jwt/dev-public.pem
